kind = "web"
previewPath = "/"
title = "The Roof Store"
version = "1.0.0"
id = "artifacts/roof-store"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24639

[services.development]
run = "pnpm --filter @workspace/roof-store run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/roof-store", "run", "build" ]
run = [ "node", "artifacts/roof-store/server.js" ]

[services.env]
PORT = "24639"
BASE_PATH = "/"
